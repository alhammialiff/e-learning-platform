import { InsertCreateSectionComponentDirective } from './insert-create-section-component.directive';

describe('InsertCreateSectionComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertCreateSectionComponentDirective();
    expect(directive).toBeTruthy();
  });
});
